[project]
name = "mcp-ollama"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12.3",
    "mcp[sse]>=1.14.1",
    "ollama>=0.5.4",
]

[tool.uv.workspace]
members = [
    "servers/web-search",
]
